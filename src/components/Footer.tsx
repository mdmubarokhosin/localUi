import { FC } from 'react';

export const Footer: FC = () => {
  return (
    <footer
      className="w-full max-md:pb-4 py-1 text-base-content/50 text-xs text-center"
      tabIndex={0}
    >
    </footer>
  );
};
